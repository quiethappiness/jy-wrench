---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by ZhouLele.
--- DateTime: 2025/11/2 10:58
---
local function safe_tonumber(str)
    if type(str) == "number" then
        return str
    end
    if type(str) ~= "string" then
        return nil
    end

    -- 移除非数字字符（保留负号和小数点）
    str = string.gsub(str, "[^0-9%.%-]", "")
    return tonumber(str)
end
local resultKey = KEYS[1]
local resultValue = ARGV[1]
local expireTime = safe_tonumber(ARGV[2])
redis.call('HSET', resultKey, 'result', resultValue)
redis.call('EXPIRE', resultKey, expireTime)
return 1