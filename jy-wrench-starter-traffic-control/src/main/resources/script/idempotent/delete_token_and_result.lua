---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by ZhouLele.
--- DateTime: 2025/11/2 15:15
---
-- 安全转换函数
local function safe_tonumber(str)
    if type(str) == "number" then
        return str
    end
    if type(str) ~= "string" then
        return nil
    end

    -- 移除非数字字符（保留负号和小数点）
    str = string.gsub(str, "[^0-9%.%-]", "")
    return tonumber(str)
end
local tokenKey = KEYS[1]
local resultKey = KEYS[2]
local expireTime = safe_tonumber(ARGV[1])
local r1= redis.call('EXPIRE', tokenKey, expireTime)
local r2 = redis.call('EXPIRE', resultKey, expireTime)
return r1 + r2