server:
    port: 9191

# 小傅哥扳手通用组件配置
jy:
    wrench:
        config:
            system: test-system
            register:
                # redis host
                host: s-pay-mall.top
                # redis port
                port: 16379

# 日志
logging:
    level:
        root: debug
    config: classpath:logback-spring.xml